# Slike - dodaj lokalno, ne commituj (osim SVG resursa u flags/)
images/*
!images/flags/

# Originalni zip
*.zip

# Razvojne skripte (lokalne, ne idu na server)
*.ps1

# Referentni materijal sa starog sajta - ne ide na GitHub
uslovi/

# PHP error logovi (generisu se na serveru)
error_log
**/error_log
*.log
